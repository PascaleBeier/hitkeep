{
  "servers": {
    "hitkeepDev": {
      "type": "stdio",
      "command": "${workspaceFolder}/hk",
      "args": ["--workspace", "${workspaceFolder}", "mcp", "serve"],
      "cwd": "${workspaceFolder}"
    }
  }
}
